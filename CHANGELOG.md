# Changelog

All notable changes to `@absolutejs/egress`.

This file is generated by `absolute-changelog` from the entries in
`changelog/`. Edit an entry, not this file — and add new ones under
`changelog/unreleased/`.

## 0.3.1 — 2026-09-24

### Fixed

- **Honor the all-addresses DNS lookup callback used by Node and Bun connection racing while retaining the single validated public address pin.** (`pinnedPublicRequest`)

## 0.3.0 — 2026-09-24

### Added

- **Expose DNS-pinned public HTTP/HTTPS transport while preserving allowlisted HTTPS egress policy**

### Changed

- **Explicitly preserve the public manifest contract instead of exposing dependency-inferred settings types** (`manifest`)
